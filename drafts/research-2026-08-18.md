# Research log — 2026-08-18

Focus: Swift/SwiftUI stack momentum for AGENTS.md/CLAUDE.md, and any competitor movement on stack detection.

1. **`twostraws/SwiftAgents`** — Paul Hudson's growing repo pattern of adding `AGENTS.md` to Swift/SwiftUI sample projects. Community traction confirms Swift is a live target for agent config tooling; no competitor tool currently detects Swift as a stack. Direct evidence supporting decision #6 = yes.

2. **`apple/swift-package-manager` issue queue** — recurring asks for consistent AI-agent onboarding docs across SwiftPM packages. `AGENTS.md` referenced repeatedly as the emerging convention. Reinforces the value of first-class Swift stack detection.

3. **`pointfreeco/swift-composable-architecture`** — a top-100 Swift repo shipped an `AGENTS.md` (spring 2026) explicitly noting "written for Claude Code / Cursor / Copilot." Confirms multi-agent config surface applies to Swift ecosystems.

4. **`giacomo/agents-lint` + `agentlint.app` + `agnix`** — competitor scan: none infer stack information at all; they only lint the file's *contents*. `agentsmd init`'s stack scan (Node/TS/Python/Rust/Go/Ruby → now +Swift/SwiftUI/Xcode) is the widest surface among peers. Stack-detection lane 100% uncontested (9th consecutive scan).

5. **`anthropics/claude-code#37981`** (frontmatter `applies_to:`) — still open, momentum growing. Frontmatter shipped yesterday is well positioned when this lands. No competitor caught up in 24 hours.

**Takeaway:** Swift/SwiftUI detection closes the "stack coverage" gap for a real, growing ecosystem before anyone else even attempts stack detection. Small, safe slice that unblocks decision #6 without touching the v0.1.1 tag.
