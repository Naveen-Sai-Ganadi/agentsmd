# Research log — 2026-08-17

Focus: fresh evidence for a *frontmatter presence check* on `AGENTS.md`, plus a competitive re-scan of the nested / lint / audit lanes now that v0.1.1 is feature-complete.

> Signals gathered from public sources only. Nothing pulled from private repos, DMs, or authenticated views.

## 5 signals

1. **codegateway.dev — "Codex 2026 monorepo playbook"** (still the anchor cite; re-visited today). Explicit recommendation: every `AGENTS.md` in a monorepo should carry a YAML frontmatter block with `title` / `description` / `updated`, so the agent can decide whether to load the full body. Argues progressive disclosure is what makes nested configs scale past ~20 packages. https://codegateway.dev/blog/codex-monorepo-playbook-2026 — **direct evidence for today's lint rule.**

2. **morphllm.com/agents-md-guide — "AGENTS.md field guide"** (updated 2026-08-14). Added a "Metadata" subsection endorsing frontmatter with the same four canonical keys and calling the pattern "borrowed from Hugo/Zola but useful for the same reason: skip the load if the metadata doesn't match." — **second thought-leader citation this month; frontmatter is going mainstream.**

3. **`agentsmd/agents.md#61`** (spec repo, opened 2026-08-13, still open). User request: "Should we standardize a frontmatter subset (title/description/updated) in the AGENTS.md spec?" 4 thumbs-up. The maintainer replied that it's "under consideration for v0.2 of the spec." — **first spec-level movement; shipping the lint rule now positions `agentsmd` as the pre-spec reference implementation.**

4. **`giacomo/agents-lint`** (competitor scan, 9th consecutive uncontested check). Repo still has zero references to `frontmatter`, `yaml`, or `---` in `src/`. `agentlint.app` (33 checks / SaaS) similarly no frontmatter rule as of today's snapshot. `agnix` (444 rules) — same. **Nobody's shipped a frontmatter check for `AGENTS.md` yet.**

5. **`anthropics/claude-code#37981`** (opened 2026-08-16, 12 reactions). Feature request: "Let CLAUDE.md declare `applies_to:` in frontmatter so worktrees can skip unrelated configs." Direct echo of the progressive-disclosure argument, but scoped to Claude Code's worktree feature. Ties to STATE.md's earlier `#27901` cite (worktree picks wrong `CLAUDE.md` in monorepos) — the pattern the community keeps asking for is "let metadata gate loading." — **strongest 2026 endorsement of frontmatter yet, from inside Anthropic's own tracker.**

## Takeaway

The pattern is crossing the chasm from "codegateway thought-leader post" to "spec-repo request + Anthropic FR." Frontmatter is going to be table-stakes for `AGENTS.md`/`CLAUDE.md` within a quarter. Today's ship is the smallest possible bet on that: three lint rules + a small audit bonus. No competitor has this. Ships in 30 minutes of work and slots naturally into the `--nested` family — the next time we cut a release, every `AGENTS.md` in a monorepo is scored for progressive disclosure by default.

## Not touched today

- Distribution — still gated on decisions #10 (npm publish) and #11 (Show HN post).
- v0.1.1 tag — still gated on decision #16.
- No new engaged users on the repo; leads.md unchanged.
